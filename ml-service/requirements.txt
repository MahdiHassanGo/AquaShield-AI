--extra-index-url https://download.pytorch.org/whl/cpu
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
python-multipart>=0.0.20,<1
pillow>=11,<13
timm>=1.0,<2

# Use CPU-only torch on Render/Linux to prevent 2GB+ CUDA package download and build timeout
torch>=2.5,<3; platform_system != "Darwin"
torchvision>=0.20,<1; platform_system != "Darwin"

# Standard torch on macOS (Darwin) for your local development
torch>=2.5,<3; platform_system == "Darwin"
torchvision>=0.20,<1; platform_system == "Darwin"
